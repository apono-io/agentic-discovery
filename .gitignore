# generated reports must never be committed -- unredacted ones contain real resource names
agentic-access-report-*
node_modules/
__pycache__/
*.pyc
.DS_Store
# internal strategy notes -- kept local, not part of the tool
RESEARCH-NOTES.md

# build artifacts -- binaries ship via GitHub Releases, never in git history
dist/
build/.work/
agentic-access-assessment-*
assessment.html
Reports/

# the single-file bundle IS committed on purpose -- it is what the one-liner fetches
!agentic-discovery.cjs
